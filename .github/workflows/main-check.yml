name: main-check

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Prepare environment
      run: sudo apt-get update && sudo apt-get install libboost-system-dev

    - name: Configure CMake
      run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} -DBUILD_TESTS=ON

    - name: Build
      run: cmake --build ${{github.workspace}}/build --config ${{env.BUILD_TYPE}}

  test:
    runs-on: ubuntu-latest
    needs: [build]

    - name: Test
      working-directory: ${{github.workspace}}/build
      run: ctest -C ${{env.BUILD_TYPE}} --output-on-failure
    
  code-formatting:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: clang-format
      run: find ./source ./test -iname '*.cpp' -o -iname '*.hpp' | xargs clang-format -n -Werror --ferror-limit=1

        # static-analysis:
        # needs: [build-and-test]
        # runs-on: ubuntu-latest
    
        # steps:
        # - uses: actions/checkout@v3

        # - name: Prepare environment
        # run: sudo apt-get update && sudo apt-get install clang-format
    
        # - name: clang-tidy
        # run: run-clang-tidy ./source/
